%h1 Currently registered user list
%table.table.table-bordered.table-hover
  %thead
    %tr
      %th Username
      %th Created at
  %tbody
    - User.all.each do |user|
      %tr
        %td= user.email
        %td= user.created_at
